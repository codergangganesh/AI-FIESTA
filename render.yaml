services:
  - type: web
    name: ai-fiesta
    env: node
    plan: free
    buildCommand: npm install; npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_VERSION
        value: 20
